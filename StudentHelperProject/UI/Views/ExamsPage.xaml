<Page x:Class="StudentHelper.WPF.UI.Views.ExamsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Екзамени"
                   FontSize="32"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Tabs -->
        <TabControl x:Name="Tabs"
                    Grid.Row="1"
                    Height="40"
                    Margin="0,0,0,10"
                    SelectionChanged="Tabs_Changed">
            <TabItem Header="Поточні"/>
            <TabItem Header="Минулі"/>
        </TabControl>

        <!-- CONTENT BOX -->
        <Border Grid.Row="2"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="8"
                Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <!-- Subject filter з кнопкою додавання -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <ComboBox x:Name="SubjectFilter"
                                  Height="34"
                                  Width="180"
                                  FontSize="14"
                                  SelectionChanged="SubjectFilter_Changed">
                            <ComboBoxItem Content="Всі предмети" IsSelected="True"/>
                        </ComboBox>

                        <Button Content="+"
                                Width="34"
                                Height="34"
                                Margin="8,0,0,0"
                                Background="#B3F2FF"
                                BorderBrush="Black"
                                Click="AddSubject_Click"
                                ToolTip="Додати новий предмет"/>
                    </StackPanel>

                    <!-- Exams list -->
                    <ItemsControl x:Name="ExamList"/>

                    <!-- Add exam -->
                    <TextBlock Text="Додати екзамен"
                               Foreground="DodgerBlue"
                               FontSize="16"
                               Margin="0,20,0,0"
                               Cursor="Hand"
                               MouseLeftButtonUp="AddExam_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>