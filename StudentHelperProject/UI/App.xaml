<Application x:Class="StudentHelper.WPF.UI.App"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

<Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Конвертери -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- Основні кольори -->
            <Color x:Key="PrimaryColor">#FF4C6FFF</Color>
            <Color x:Key="AccentColor">#FFFFB347</Color>
            <Color x:Key="SuccessColor">#28A745</Color>
            <Color x:Key="WarningColor">#FFC107</Color>
            <Color x:Key="ErrorColor">#DC3545</Color>
            <Color x:Key="InfoColor">#17A2B8</Color>
            <!-- Кольори для навігаційних кнопок -->
            <SolidColorBrush x:Key="ActiveNavButtonBackground" Color="#9ADDEA"/>
            <SolidColorBrush x:Key="ActiveNavButtonBorder" Color="DarkBlue"/>
            <SolidColorBrush x:Key="InactiveNavButtonBackground" Color="#B3F2FF"/>
            <SolidColorBrush x:Key="InactiveNavButtonBorder" Color="Black"/>

            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
            <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />
            <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}" />
            <SolidColorBrush x:Key="BackgroundBrush" Color="#FFF5F6FA" />

            <!-- Стиль для діалогових кнопок -->
            <SolidColorBrush x:Key="DialogButtonBackground" Color="#B8F1F1"/>
            <SolidColorBrush x:Key="DialogButtonBorder" Color="Black"/>

            <Style x:Key="DialogButton" TargetType="Button">
                <Setter Property="Margin" Value="0,4"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="Background" Value="{StaticResource DialogButtonBackground}"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="BorderBrush" Value="{StaticResource DialogButtonBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#9FE0E0"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>